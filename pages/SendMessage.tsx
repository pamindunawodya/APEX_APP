import { Button, Text, View } from "react-native";
import React, { useState } from 'react';
import App from "../App";

const SendMessage = () => {

     
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>This is the SendMessage</Text>
            
        </View>
    );
}

export default SendMessage;
